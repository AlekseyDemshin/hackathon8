const iframe = `
// ###code place###
`;

function addIFrame(container) {
  container.append;
}

function bootstrap() {
  const container = document.getElementById("container");
  addIFrame(container);
}

miro.onReady(bootstrap);
